# 验证记录

验证日期：2026-07-31

目标版本：0.3.3

目标浏览器：Chromium（Chrome / Edge）

## 0.3.3 最终回答切换保护

本版本针对一个特定问题：长时间思考过程能够持续显示，但思考完成、页面切换到最终回答时可能停在旧状态，刷新页面后才看到结果。

0.3.2 已经保护最新消息和最新思考段不进入显式 `hidden` 冻结，但这些元素仍可能同时受到首屏样式和运行时样式的 `content-visibility: auto` 影响。0.3.3 在核心优化脚本之后增加一层独立切换保护：

- 最新 4 条消息的外层强制使用 `content-visibility: visible`。
- 稳妥、均衡、强力模式分别保留最新 12、8、5 个思考 / 正文块为完整渲染。
- 更早的长思考块仍继续使用原有自动离屏优化或远距离冻结。
- 思考区域折叠、最终正文插入或替换等块级结构变化会触发 60ms 防抖更新。
- 不监听 `characterData`，不会因每个流式 token 重新扫描。
- 不读取聊天正文，不发起网络请求，不增加权限。

真实 ChatGPT 的思考态到最终回答切换由站点自身实现，合成页面无法完全复刻该状态机。因此本版本完成静态、语法和边界检查后，仍需要在真实长会话中手动验证最终切换是否恢复正常。

## 自动化检查

运行：

```powershell
node tests/static-check.mjs
node tests/recovery-check.mjs
```

结果：

- Manifest V3 格式通过，版本号和 `package.json` 均为 0.3.3，权限仍严格限制为 `storage` 和 `activeTab`。
- 仅匹配 `chatgpt.com` 与旧版 `chat.openai.com`，没有额外主机权限。
- `early.css` 与 `early.js` 在 `document_start` 注入，主内容脚本和最终回答切换保护在 `document_idle` 依次加载。
- 运行代码未包含 `fetch`、XHR、WebSocket、EventSource 或 Beacon。
- 内容脚本不读取 `innerText`；唯一的 `textContent` 读取被限制在候选错误提示的前 1000 个字符，用于本地分类，不扫描聊天正文。
- 最终回答切换保护不读取 `innerText` 或聊天节点的 `textContent`。
- 后续扫描不包含 `getBoundingClientRect` / `getComputedStyle`，不会集中触发同步布局。
- 两层 MutationObserver 都不监听 `characterData`，流式生成的每个文字变化不会触发整页重扫。
- 切换保护会清除最新消息和最新正文块上可能残留的显式冻结标记。
- 救援流程测试确认：复制当前 ChatGPT 对话地址、在相邻标签页打开、休眠旧页，并拒绝非 ChatGPT 地址。

## 0.3.3 手动测试重点

1. 在浏览器扩展页面重新加载 0.3.3。
2. 刷新消息数量较多、且扩展处于均衡或强力模式的 ChatGPT 会话。
3. 发起需要较长时间思考的请求，确认思考过程仍能持续显示。
4. 重点观察思考完成后，最终回答是否立即开始输出或显示。
5. 在开发者工具中检查最新会话外层是否带 `data-glco-transition-live="true"`。
6. 检查最终回答最新正文块是否带 `data-glco-transition-block="true"`。
7. 检查上述元素的计算样式为 `content-visibility: visible`。
8. 向上滚动检查较早思考段仍可按原逻辑恢复，确认没有把整条长思考全部取消优化。

## 弹窗布局与交互回归

使用 `tests/popup-visual-check.mjs` 在 Edge 无头模式中按扩展弹窗的 340 × 600 CSS 视口渲染：

| 检查项 | 结果 |
| --- | --- |
| 页面内容高度 | 551px，低于 600px 视口 |
| 根节点滚动尺寸 | 340 × 600，无横向或纵向溢出 |
| 三档模式行高 | 均为 38px |
| 门槛说明对齐 | 右边缘均为 311px，距卡片右边缘 11px |
| 指针选择 | 可切换到“强力” |
| 键盘选择 | `ArrowUp` 可从“强力”返回“均衡” |
| 启用开关 | 暂停与恢复状态均正确 |
| 页面救援 | 成功结果正确显示 |
| 深色模式 | 340 × 600 下仍无溢出 |
| 控制台 | 无 `pageerror` 或 error 日志 |

同尺寸明暗截图和原版对照图保存在 `artifacts/design-qa`；完整视觉核对见 [design-qa.md](design-qa.md)。

## 首屏与连续刷新压力测试

使用 `tests/fixture.html` 构造 4 条普通消息和一条包含 1200 个思考段的回复。该规模用于放大真实页面重建阶段的布局与观察器压力。

| 场景 | 结果 |
| --- | --- |
| 首次加载 | `glcoStartup=true`，1200 个思考段均为 `content-visibility:auto` |
| 分批接管 | 1200 个块全部被追踪，页面状态为 `active` |
| 连续刷新 3 次 | 3 次均完成加载与分批接管，健康状态保持 `none`，无崩溃或脚本错误 |
| 单次刷新观察窗口 | 包含固定 800ms 等待时，每次总耗时为 929–953ms |
| 本地控制台 | 无 warning / error |

这些数字来自 0.3.2 合成页面基线，不能证明真实 ChatGPT、网络或服务端一定不会失败；它们验证的是扩展自身不会在极长回复刷新时制造集中几何测量峰值。0.3.3 没有改动旧消息或旧思考段的核心优化算法。

## 功能与回归测试

| 场景 | 结果 |
| --- | --- |
| 120 段长思考，均衡模式 | 121 个语义块被追踪，首屏保护与增强层均启用 |
| 流式追加 20 段 | 页面从 120 增至 140 个思考段，追踪单元同步更新为 141 |
| 暂时显示全部 | 状态变为 `paused`，计算样式为 `visible`；计时结束后恢复为 `active` / `auto` |
| 稳妥模式 | 121 个块使用自动离屏优化，显式冻结为 0 |
| 10 段、均衡模式 | 保持 `waiting`，但首屏 CSS 仍为 `auto` |
| 10 段、强力模式 | 11 个语义块被追踪，增强层启动 |
| 禁用状态 | `glcoStartup=false`、状态为 `disabled`、无追踪单元，计算样式为 `visible` |
| 普通 `[role="alert"]` 通知 | “已复制到剪贴板”保持 `healthIssue=none`，不显示红点 |
| CSS 隐藏的连接错误 | `display:none` 的 “Network error” 保持 `healthIssue=none` |
| 屏外连接错误 | 位于视口外的旧 “Network error” 保持 `healthIssue=none` |
| 可见连接错误 | “Synthetic connection error” 分类为 `connection-error` |
| 明确页面错误 | `data-state="error"` 分类为 `page-error` |
| 错误节点移除 | 状态在 MutationObserver 复检后恢复为 `none` |
| 错误仅被 CSS 隐藏 | 无 DOM 增删时，3 秒定时复检仍将状态恢复为 `none` |
| 180 条长会话 | 180 条消息全部追踪；滚动往返后，9 条已测量旧消息显式冻结 |
| 180 条滚动稳定性 | 页面高度从 76,986px 变为 76,891px，变化约 0.12% |
| 嵌套滚动容器 | `main` 滚动至 33,371px，窗口保持 0，状态保持 `active` |

v0.3.3 不再为了得到较大的“冻结数量”而预先测量所有离屏内容。未访问过的远处内容由浏览器原生 `content-visibility:auto` 处理；只有进入过视口缓冲区并获得可靠高度的内容，离开后才会显式冻结。切换保护只覆盖最新交互区域，不改变该原则。

## 布局压力对照

压测页连续 60 次改变会话栏宽度并强制读取布局，用于模拟窗口、侧栏或界面状态变化造成的全局重排。

### 300 条消息

| 执行顺序 | 状态 | 平均单次布局 | P95 |
| --- | --- | ---: | ---: |
| 第一轮 | 启用扩展 | 1.407ms | 2.9ms |
| 第一轮 | 禁用扩展 | 8.980ms | 10.8ms |
| 反向复核 | 禁用扩展 | 9.442ms | 13.9ms |
| 反向复核 | 启用扩展 | 1.020ms | 1.5ms |

v0.3.1 追加回归为平均 1.073ms、P95 1.3ms，健康检测改动未造成可见性能回退。

### 120 段长思考

| 状态 | 平均单次布局 | P95 |
| --- | ---: | ---: |
| 启用扩展 | 0.640ms | 1.1ms |
| 禁用扩展 | 1.680ms | 2.5ms |

在这组受控测试中，300 条消息的平均布局开销下降约 6.4–9.3 倍，120 段长思考下降约 62%。结果只证明优化机制在合成页面中的效果，不代表不同硬件和真实会话都能获得相同倍数。0.3.3 的真实最终回答切换仍以手动验证结果为准。

## 救援边界

“救援重开”已经通过后台脚本模拟测试，但未在自动化测试中连接真实 ChatGPT 账号。该功能只自动化以下浏览器操作：

1. 在新标签页打开当前 ChatGPT 地址。
2. 新标签页创建成功后休眠旧标签页。
3. 旧标签页保留，不自动关闭。

它不能修复 OpenAI 服务端错误、网络路由、VPN / 代理、WebSocket 或其他长连接问题，也不保证恢复未发送的输入。
