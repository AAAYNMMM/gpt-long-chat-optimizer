# 验证记录

验证日期：2026-07-31

目标版本：0.2.0

目标浏览器：Chromium（Chrome / Edge）

## 0.2.0 修改目标

修复超长会话中，长时间思考已经结束，但页面从思考过程切换到最终回答时没有及时刷新的问题。

本版本不增加超时判断或自动刷新，而是将最新 4 条消息和仍带有生成标记的消息完全排除在 `content-visibility` 优化之外。

## 静态检查

运行：

```powershell
node tests/static-check.mjs
```

检查范围：

- Manifest V3 格式与 0.2.0 版本号。
- JavaScript 语法。
- 权限仍严格限制为 `storage` 和 `activeTab`。
- 仅匹配 `chatgpt.com` 与旧版 `chat.openai.com`。
- 运行代码不包含额外网络请求。
- 内容脚本不读取聊天文字。
- 原有自动离屏跳过和远距离冻结仍然存在。
- `live-turn-protection.js` 在核心优化脚本之后加载。
- 最新 4 条消息使用 `content-visibility: visible`。
- 实时消息会移除可能残留的远距离冻结标记。

新增脚本已完成语法解析与关键规则检查。真实 ChatGPT 的思考结束切换场景需要在浏览器中手动验证，因为合成压测页无法完整复现 ChatGPT 自身的 React 状态切换。

## 0.2.0 手动测试重点

1. 打开消息数量超过当前模式门槛的长会话。
2. 确认扩展处于启用状态。
3. 发起会触发较长思考的请求。
4. 观察思考过程是否正常显示。
5. 重点观察思考结束后，最终回答是否立即开始显示。
6. 分别测试均衡模式与强力模式。
7. 在开发者工具中确认最新 4 条会话节点带有 `data-glco-live="true"`。
8. 检查最新消息的计算样式为 `content-visibility: visible`。

## 0.1.0 基线结果

以下结果来自 2026-07-30 的 0.1.0 合成长会话测试，用于确认原有性能优化没有被有意移除。

| 场景 | 结果 |
| --- | --- |
| 180 条，均衡模式，页面顶部 | 180 条全部被追踪，143 条远距离消息冻结 |
| 滚动至中段 | 视口最近 5 条全部已恢复；页面高度从 53,909px 变为 53,906px |
| 动态追加 20 条 | 总数更新为 200；新增且已远离视口的 10 条被冻结；最近 10 条保持完整 |
| 临时显示全部 | 冻结数量立即变为 0、状态变为 paused；测试计时结束后自动恢复为 active |
| 稳妥模式 | 90 条被追踪，显式冻结为 0 |
| 低于门槛 | 20 条时状态为 waiting，不改变消息元素 |
| 禁用状态 | 180 条时状态为 disabled，不改变消息元素 |
| 备用选择器 | 删除所有 `conversation-turn-*` 属性后，仍通过 `data-message-author-role` 识别 80 条 |
| 嵌套滚动容器 | 视口附近消息正常恢复，容器高度仅变化 3px |
| 本地控制台 | 无来自压测页或扩展脚本的 warning / error |

0.1.0 的 300 条消息合成布局压力测试中，启用扩展后的平均全局布局开销约为原始页面的八分之一到六分之一。0.2.0 保留原有旧消息优化，仅让最新 4 条消息承担必要的实时布局与绘制。
