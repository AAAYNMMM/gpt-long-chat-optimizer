# 验证记录

验证日期：2026-07-30

目标版本：0.1.0

目标浏览器：Chromium（Chrome / Edge）

## 静态检查

运行：

```powershell
node tests/static-check.mjs
```

结果：

- Manifest V3 格式通过。
- JavaScript 语法通过。
- 权限严格限制为 `storage` 和 `activeTab`。
- 仅匹配 `chatgpt.com` 与旧版 `chat.openai.com`。
- 运行代码未包含 `fetch`、XHR、WebSocket、EventSource 或 Beacon。
- 内容脚本未读取 `innerText` 或聊天节点的 `textContent`。
- 核心 CSS 同时包含自动离屏跳过与显式远距离冻结。

## 浏览器集成测试

使用 `tests/fixture.html` 构造与 ChatGPT 相同关键属性的长会话。

| 场景 | 结果 |
| --- | --- |
| 180 条，均衡模式，页面顶部 | 180 条全部被追踪，143 条远距离消息冻结 |
| 滚动至中段 | 视口最近 5 条全部已恢复；页面高度从 53,909px 变为 53,906px |
| 动态追加 20 条 | 总数更新为 200；新增且已远离视口的 10 条被冻结；最近 10 条保持完整 |
| 临时显示全部 | 冻结数量立即变为 0、状态变为 paused；测试计时结束后自动恢复为 active |
| 稳妥模式 | 90 条被追踪，显式冻结为 0 |
| 低于门槛 | 20 条时状态为 waiting，不改变消息元素 |
| 禁用状态 | 180 条时状态为 disabled，不改变消息元素 |
| 备用选择器 | 删除所有 `conversation-turn-*` 属性后，仍通过 `data-message-author-role` 识别 80 条 |
| 嵌套滚动容器 | `main` 独立滚动到 21,999px，窗口保持 0；视口附近消息正常恢复，容器高度仅变化 3px |
| 本地控制台 | 无来自压测页或扩展脚本的 warning / error |

## 合成布局压力测试

压测在 300 条消息上连续改变会话栏宽度并强制读取布局，共 60 次。该场景模拟长会话在窗口变化、侧栏变化或界面更新时发生的全局重排。

| 执行顺序 | 状态 | 平均单次布局 | P95 |
| --- | --- | ---: | ---: |
| 第一轮 | 启用扩展 | 1.247ms | 2.4ms |
| 第一轮 | 原始页面 | 8.342ms | 13.5ms |
| 反向复核 | 原始页面 | 8.090ms | 12.5ms |
| 反向复核 | 启用扩展 | 1.007ms | 1.2ms |

在此合成页面中，平均全局布局开销下降约 6.7–8.0 倍。测试同时确认 300 条消息中有 263 条远距离消息被冻结。

这组数字只证明优化机制在受控长页面上的效果，不代表真实 ChatGPT、不同硬件或不同会话内容都能获得相同倍数。简单静态滚动压测没有显示有意义的差异；本扩展的主要收益来自减少长页面的布局、样式计算与绘制，而不是改变滚轮事件本身。
