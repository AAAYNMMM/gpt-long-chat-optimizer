# 隐私说明

GPT 长聊加速器的处理完全发生在浏览器本地。

## 扩展会处理什么

- ChatGPT 会话中消息及长回复内部块级元素的位置与高度。
- 当前页面的消息元素数量。
- 当前被冻结的离屏消息与长回复段落数量。
- 浏览器是否在线，以及页面中是否存在带 `alert` / `error` 标记的提示元素；扩展不读取该提示的文字。
- 用户选择的启用状态和优化强度。
- 当用户主动点击“救援重开”时，处理当前 ChatGPT 标签页的地址、标签页编号与位置，用于打开相同地址并休眠旧标签页。

## 扩展不会处理什么

- 不提取、读取或记录消息文本。
- 不读取输入框内容。
- 不访问账号资料、Cookie、Local Storage 或浏览历史。
- 不向开发者或任何第三方发送数据，也不使用 `fetch`、XHR、WebSocket、Beacon 等接口。
- 不包含分析、广告或遥测代码。

“救援重开”会让浏览器正常打开当前 ChatGPT 地址，因此会产生与用户手动打开该页面相同的站点网络请求；扩展不会额外上传数据。旧标签页会被 Chromium 休眠而不是关闭，未发送的输入不保证能够恢复。

浏览器同步存储中仅保存以下两项设置：

```json
{
  "enabled": true,
  "mode": "balanced"
}
```

卸载扩展会停止所有页面处理。浏览器是否同步或保留扩展设置，由 Chrome / Edge 自身的扩展数据策略决定。
