# 更新日志

## 0.3.3

- 修复长时间思考完成后，从思考过程切换到最终回答时页面可能不刷新的问题。
- 最新 4 条消息外层改为普通完整渲染，避免最终回答切换被外层 `content-visibility: auto` 延迟。
- 只将各模式保留的最新思考 / 正文块设为完整渲染，更早的长思考段仍继续离屏优化。
- 新插入或替换的最终回答块会在块级结构变化后重新标记，不监听每个流式文字变化。
- 切换保护不读取聊天正文、不发起网络请求，也不增加扩展权限。

## 0.3.2

- 将三档优化说明移到选项右侧，同一行展示模式名称与门槛。
- 缩短重复说明并收紧弹窗纵向间距，在 340 × 600 的常见弹窗视口中无需滚动即可完成主要操作。
- 保留整行可点击、键盘焦点、选中态及深色模式表现。

## 0.3.1

- 修复正常页面长期显示“页面出现异常提示”红点的问题。
- 普通 `[role="alert"]` 现在必须实际可见且匹配连接 / 生成错误文字才会触发。
- 明确的 `data-testid*="error"` 与 `data-state="error"` 仍可被识别为页面错误。
- 新增 CSS 与视口可见性检查，隐藏或已滚出视口的旧错误节点不会触发状态。
- 错误状态每 3 秒自动复检，提示消失或被隐藏后会自行清除。
- 新增普通通知、隐藏错误、连接错误、显式页面错误及定时清除回归场景。

## 0.3.0

- 新增 `document_start` 首屏保护，在 ChatGPT 重建长对话和长思考内容时提前启用离屏渲染优化。
- 移除首次扫描中的集中几何读取，改为分批识别、只测量视口缓冲区内的内容。
- 不再监听每次流式文字变化；只在块级结构变化时重新分段，降低长时间思考期间的观察器开销。
- 新增“救援重开当前对话”：在新标签页打开相同地址，并休眠旧标签页释放内存。
- 新增浏览器在线状态与页面错误提示元素检测，异常时在扩展图标和弹窗中提示。
- 增加救援流程自动化测试，以及 1200 段长思考连续刷新压力测试。

## 0.2.0

- 新增单条超长思考 / 回复的消息内分块优化。
- 流式生成期间保留最新思考段，对远离视口的旧段落进行轻量化。
- 新增长思考独立门槛，即使会话总消息数很少也能生效。
- 弹窗新增“思考旧段”状态指标。
- “暂时显示全部”、页内查找和打印现在同时恢复消息与消息内分段。
- 压测页新增流式思考追加、完成态和长思考布局测试。

## 0.1.0

- 首个版本。
- 支持按整条消息优化超长 ChatGPT 会话。
- 提供稳妥、均衡和强力三种模式。
